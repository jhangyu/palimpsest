---
// ---
// name: feeds-add
// description: "Add new feed wizard: URL and site name input, AI-assisted list rule and content rule generation with live crawl preview, and save button to start crawling"
// type: page
// target:
//   layer: frontend
//   domain: feeds
// spec_doc: null
// test_file: tests/stage2/e2e/stage2/feeds.spec.ts
// route: /feeds/add
// ---
import AdminLayout from '../../layouts/admin/admin-layout.astro'
import PageHeader from '../../components/common/page-header.astro'
import FeedRulesEditor from '../../components/feeds/FeedRulesEditor.astro'

const headerProps = {
  title: 'Create New Feed',
  breadcrumbs: [
    { label: 'Dashboard', href: '/dashboard' },
    { label: 'Feeds', href: '/dashboard' },
    { label: 'Add', class: 'active' }
  ],
  actions: [
    {
      label: 'Back',
      href: '/dashboard',
      type: 'link' as const,
      variant: 'btn-outline-secondary' as const,
      icon: 'ri-arrow-left-line'
    }
  ]
}
---

<AdminLayout
  currentPath="feeds/add"
  title="Create New Feed"
  description="Add a new website feed with AI-powered extraction rules"
>
  <PageHeader {...headerProps} />

  <div id="feed-wizard">
    <div class="row g-4">
      <!-- Form column -->
      <div class="col-12" id="form-col">
        <div class="card">
          <div class="card-body">
            <h5 class="card-title mb-1">Feed Configuration</h5>
            <p class="text-muted small mb-4">
              Provide the website details and let AI figure out the extraction rules.
            </p>

            <!-- URL + Name -->
            <div class="row g-3 mb-3">
              <div class="col-md-8">
                <label for="wizard-url" class="form-label">Target Website URL / RSS</label>
                <div class="d-flex align-items-center">
                  <input
                    type="url"
                    class="form-control"
                    id="wizard-url"
                    placeholder="https://example.com/blog"
                  />
                  <div id="source-type-toggle" class="btn-group btn-group-sm ms-2" role="group">
                    <button
                      type="button"
                      id="source-type-html"
                      class="btn btn-outline-secondary active">HTML</button
                    >
                    <button type="button" id="source-type-rss" class="btn btn-outline-secondary"
                      >RSS</button
                    >
                  </div>
                </div>
              </div>
              <div class="col-md-4">
                <label for="wizard-name" class="form-label">Site Name</label>
                <input
                  type="text"
                  class="form-control"
                  id="wizard-name"
                  placeholder="Example Blog"
                />
              </div>
            </div>

            <hr />

            <FeedRulesEditor
              cmListId="cm-list-rules"
              cmContentId="cm-content-rules"
              sampleUrlId="input-sample-url"
            />

            <!-- Page-specific footer: Debug switch, Test Both, Save -->
            <div class="d-flex justify-content-end align-items-center gap-3 mt-2">
              <!-- Debug toggle (Bootstrap switch) -->
              <div class="form-check form-switch d-flex align-items-center gap-2 mb-0">
                <input class="form-check-input" type="checkbox" role="switch" id="wizard-debug" />
                <label class="form-check-label d-flex align-items-center gap-1" for="wizard-debug">
                  <i class="ri-bug-line"></i> Debug
                </label>
              </div>

              <!-- Test Both -->
              <button class="btn btn-primary" id="btn-test-both" type="button">
                <i class="ri-file-list-line me-1"></i> Test Both
              </button>

              <!-- Save -->
              <button class="btn btn-success" id="btn-save" type="button">
                <i class="ri-rocket-line me-1"></i> Start Crawling
              </button>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Preview Section (hidden until test) -->
    <div class="card mt-4 d-none" id="preview-section">
      <div class="card-header d-flex justify-content-between align-items-center">
        <h5 class="card-title mb-0">
          <i class="ri-eye-line me-1"></i> Live Crawl Preview
        </h5>
      </div>
      <div class="card-body">
        <!-- Debug banner -->
        <div class="alert alert-warning d-none d-flex align-items-center gap-2" id="debug-banner">
          <i class="ri-bug-line"></i>
          <span>Debug artifacts saved to:</span>
          <code id="debug-banner-path"></code>
        </div>

        <!-- Error -->
        <div class="alert alert-danger d-none" id="preview-error"></div>

        <!-- Loading -->
        <div class="text-center py-4 d-none" id="preview-loading">
          <div class="spinner-border text-primary mb-2" role="status">
            <span class="visually-hidden">Loading...</span>
          </div>
          <p class="text-muted preview-loading-text">Crawling and analyzing...</p>
        </div>

        <!-- Results table -->
        <div id="preview-body"></div>
      </div>
    </div>
  </div>
</AdminLayout>

<script>
  import { initFeedWizard } from '@/scripts/feed-wizard'

  document.addEventListener('astro:page-load', () => initFeedWizard())
</script>
