---
import Layout from '../../layouts/Layout.astro';
import EditFeedPage from '../../components/EditFeedPage.jsx';

export const prerender = false;
const { id } = Astro.params;
---

<Layout>
  <EditFeedPage client:only="react" initialSiteId={id} />
</Layout>
